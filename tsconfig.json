{
  "compilerOptions": {
    "outDir": "./dist/",
    "noImplicitAny": true, // Не дает использовать переменные без явного указания типа
    "module": "ESNext",
    "target": "es5", // в какую версию будем компилировать es5 - для поддержки браузеров
    "jsx": "react",
    "allowJs": true, // Обработка не только TS, но и JS файлов
    "moduleResolution": "node",
    //Для абсолютного импорта
    "baseUrl": ".",
    // Вместо common.js (require) позволяет использовать импорты с (* as)
    "esModuleInterop": true,
    // Убирает у импортов (* as), превращает в обычный импорт
    "allowSyntheticDefaultImports": true
  },
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS"
    }
  }
}